<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id"> <!--sql执行完,产生的主键值 赋值给id属性-->
        INSERT INTO user(openid, name, phone, sex, id_number, avatar, create_time) values
            (#{openid},#{name},#{phone},#{sex},#{idNumber},#{avatar},#{createTime})
    </insert>
    <select id="countByMap" resultType="java.lang.Integer">
        select sum(id) from user
        <where>
            <if test="begin!=null">and #{begin} &lt; create_time</if>
            <if test="end!=null">and #{end} &gt; create_time</if>
        </where>
    </select>


</mapper>